// 삼항 연산자(ternary operator)

const array = [];
/*
let text = "";
if (array.length === 0) {
  text = "empty text";
} else {
  text = "not empty text";
}
*/

let text = array.length === 0 ? "empty text" : "not empty text";

console.log(text);
